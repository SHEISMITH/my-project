#include<stdio.h>

int sumarray(a[],size);
int main();
{
int a[4]={1,2,3,4};
int size =sizeof(array,size);
int result =sumarray(array,size);
printf("%d\n",result);
return 0
}
int sumarray(a[],size);{
int size,i,a[];
int sum=0;
for(i=0;i<size;i++);
{
sum=sum+a[i];
}
return sum
}


